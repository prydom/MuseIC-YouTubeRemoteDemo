
var main = function(){
	if (typeof player)
}

function yts_loaded (){  
  if(typeof yts === 'undefined'){
    setTimeout(yts_loaded, 10);
  }else{
    // run your code
    main();
  }  
}

yts_loaded();
